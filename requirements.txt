# requirements.txt — Evidence Rank runtime dependencies
#
# Use Python 3.11 or 3.12 in a virtual environment.
# Do not use Python 3.13 for this project: several pinned scientific packages
# and FAISS/offline dependencies may not have compatible wheels.
#
# This file is enough to reproduce the final submission with rank.py when the
# precomputed artifacts in artifacts/ are present.
#
# For full preprocessing / artifact rebuilds, install requirements-offline.txt
# after this file.

numpy==1.26.4
pandas==2.2.2
pyarrow==16.1.0
scipy==1.13.0
rank-bm25==0.2.2
pyyaml==6.0.1
polars==0.20.31
pytest==8.2.2
tqdm==4.66.4
